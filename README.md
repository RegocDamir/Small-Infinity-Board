Infinity

A single-file infinite canvas editor. No build step, just open `index.html`.
Usage:
Open `index.html` in a browser. Everything runs locally.
Features:

- Infinite pannable/zoomable canvas
- Text, image, and video nodes
- Container and stack nodes
- Flip nodes to reveal a back face
- Spawn mode: stash nodes into a mother node, release them in a pooping action.
- Connect nodes with lines
- State auto-saved.
